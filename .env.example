# Database Configuration
# Replace with your actual PostgreSQL credentials
DATABASE_URL=postgresql://postgres:password@localhost:5432/ukom_prep
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=ukom_prep

# Session Configuration
# Generate a random secret: openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-here-min-32-chars-long

# Optional: Replit Auth Configuration
# Only needed if you want OAuth login integration
# REPL_ID=your-repl-id
# ISSUER_URL=https://replit.com/oidc
